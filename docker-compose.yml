version: '3'
services:
  web:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: bravo29
