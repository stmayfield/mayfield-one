version: '3'
services:
  app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    depends_on:
      - db
  db:
    image: postgres
    ports:
      - "5432:5432"
    # volumes:
    #   postgres_data:
    environment:
      POSTGRES_PASSWORD: bravo29
